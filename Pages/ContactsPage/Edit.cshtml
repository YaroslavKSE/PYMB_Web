@page
@model PYMB_Web.Pages.ContactsPage.EditModel
@{
}
<h2>Edit</h2>
    @if (Model.errorMessage.Length > 0)
    {
        <div class="alert alert-danger" role="alert">
        @Model.errorMessage
         </div>
    }
<form method="POST">
    <input type="hidden" name="id" value = "@Model.contact.Id";
 <div class="row">
    <div class="col">
      <input type="text" class="form-control" name="name" value="@Model.contact.Name>
    </div>
    <div class="col">
      <input type="text" class="form-control" name="surname" value="@Model.contact.Surname">
    </div>
  </div>
  <div class="form-group">
    <label for="inputAddress">Email</label>
    <input type="email" class="form-control" name="email" value="@Model.contact.Email">
  </div>
  <div class="form-group">
    <label for="inputAddress2">Phone</label>
    <input type="text" class="form-control" name="phone" value="@Model.contact.Phone">
  </div>
    @if (Model.successMessage.Length > 0)
    {
        <div class="alert alert-success" role="alert">
        @Model.successMessage
         </div>
    }
    <button type="submit" class="btn btn-primary">Edit contact</button>
</form>
